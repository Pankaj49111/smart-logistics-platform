services:

  # --------------------------------------------------------------------
  # 🗄️ PostgreSQL Database
  # --------------------------------------------------------------------
  postgres:
    image: postgres:15
    container_name: postgres
    restart: always
    environment:
      POSTGRES_USER: logistics_user
      POSTGRES_PASSWORD: logistics_pass
      POSTGRES_DB: logistics_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  # --------------------------------------------------------------------
  # 🍃 MongoDB for analytics/audit
  # --------------------------------------------------------------------
  mongo:
    image: mongo:7
    container_name: mongo
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongodata:/data/db

  # --------------------------------------------------------------------
  # 📦 Kafka & Zookeeper
  # --------------------------------------------------------------------
  zookeeper:
    image: confluentinc/cp-zookeeper:7.3.0
    container_name: zookeeper
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    ports:
      - "2181:2181"
    networks:
      - smart-network

  kafka:
    image: confluentinc/cp-kafka:7.3.0
    container_name: kafka
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
    depends_on:
      - zookeeper
    networks:
      - smart-network

  # --------------------------------------------------------------------
  # 🔎 Zipkin for distributed tracing
  # --------------------------------------------------------------------
  zipkin:
    image: openzipkin/zipkin
    container_name: zipkin
    ports:
      - "9411:9411"

  # --------------------------------------------------------------------
  # 📈 Prometheus for metrics
  # --------------------------------------------------------------------
  prometheus:
    image: prom/prometheus
    container_name: prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml

  # --------------------------------------------------------------------
  # ⚙️ Config Server
  # --------------------------------------------------------------------
  config-server:
    build:
      context: ./config-server
      dockerfile: Dockerfile
    container_name: config-server
    image: smartlogistics/config-server
    ports:
      - "8888:8888"
    environment:
      - SPRING_PROFILES_ACTIVE=native
    volumes:
      - ./config-repo:/config-repo
    depends_on:
      - postgres
    networks:
      - smart-network

  # --------------------------------------------------------------------
  # 🔍 Discovery Server (Eureka)
  # --------------------------------------------------------------------
  discovery-server:
    build:
      context: ./discovery-server
      dockerfile: Dockerfile
    container_name: discovery-server
    image: smartlogistics/discovery-server
    ports:
      - "8761:8761"
    depends_on:
      - config-server
    environment:
      - SPRING_APPLICATION_NAME=discovery-server
      - SPRING_CONFIG_IMPORT=optional:configserver:http://config-server:8888
    networks:
      - smart-network

  # --------------------------------------------------------------------
  # 🌐 API Gateway
  # --------------------------------------------------------------------
  gateway-service:
    build:
      context: ./gateway-service
      dockerfile: Dockerfile
    container_name: gateway-service
    image: smartlogistics/gateway-service
    ports:
      - "8080:8080"
    depends_on:
      - discovery-server
      - config-server
    environment:
      SPRING_PROFILES_ACTIVE: docker
      SPRING_APPLICATION_NAME: gateway-service
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://discovery-server:8761/eureka/
      SPRING_CONFIG_IMPORT: optional:configserver:http://config-server:8888/
      JWT_SECRET: smartlogistics-very-long-secret-key-1234567890!
    extra_hosts:
      - "host.docker.internal:host-gateway"

    networks:
      - smart-network

  # --------------------------------------------------------------------
  # 🔐 Auth Service
  # --------------------------------------------------------------------
  auth-service:
    build:
      context: ./auth-service
      dockerfile: Dockerfile
    container_name: auth-service
    image: smartlogistics/auth-service
    ports:
      - "8081:8081"
    environment:
      SPRING_PROFILES_ACTIVE: docker
      SPRING_APPLICATION_NAME: auth-service
      SPRING_CONFIG_IMPORT: optional:configserver:http://config-server:8888/
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://discovery-server:8761/eureka/
      SPRING_DATASOURCE_URL: jdbc:postgresql://host.docker.internal:5432/authdb
      SPRING_DATASOURCE_USERNAME: authuser
      SPRING_DATASOURCE_PASSWORD: authpass
      SPRING_DATASOURCE_DRIVER_CLASS_NAME: org.postgresql.Driver
      JWT_SECRET: smartlogistics-very-long-secret-key-1234567890!
    depends_on:
      - config-server
      - discovery-server
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - smart-network

  # --------------------------------------------------------------------
  # 🚚 Shipment Service
  # --------------------------------------------------------------------
  shipment-service:
    build:
      context: ./shipment-service
      dockerfile: Dockerfile
    container_name: shipment-service
    image: smartlogistics/shipment-service
    ports:
      - "8082:8082"
    environment:
      SPRING_PROFILES_ACTIVE: docker
      SPRING_APPLICATION_NAME: shipment-service
      SPRING_CONFIG_IMPORT: optional:configserver:http://config-server:8888/
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://discovery-server:8761/eureka/
      SPRING_DATASOURCE_URL: jdbc:postgresql://host.docker.internal:5432/shipmentdb
      SPRING_DATASOURCE_USERNAME: shipmentuser
      SPRING_DATASOURCE_PASSWORD: shipmentpass
      SPRING_DATASOURCE_DRIVER_CLASS_NAME: org.postgresql.Driver
    depends_on:
      - config-server
      - discovery-server
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - smart-network

  # --------------------------------------------------------------------
  # 🚛 Fleet Service
  # --------------------------------------------------------------------
  fleet-service:
    build: ./fleet-service
    container_name: fleet-service
    ports:
      - "8083:8083"
    environment:
      SPRING_PROFILES_ACTIVE: docker
    depends_on:
      - kafka
      - postgres

  # --------------------------------------------------------------------
  # 🧠 AI Assistant Service
  # --------------------------------------------------------------------
  ai-assistant-service:
    build: ./ai-assistant-service
    container_name: ai-assistant-service
    ports:
      - "8087:8087"
    depends_on:
      - gateway-service
      - discovery-server
      - config-server
    networks:
      - smart-network
    extra_hosts:
      - "host.docker.internal:host-gateway"


  # --------------------------------------------------------------------
  # 🔔 Notification Service
  # --------------------------------------------------------------------
  notification-service:
    build: ./notification-service
    container_name: notification-service
    ports:
      - "8085:8085"
    environment:
      SPRING_PROFILES_ACTIVE: docker
    depends_on:
      - kafka

  # --------------------------------------------------------------------
  # 📊 Analytics Service
  # --------------------------------------------------------------------
  analytics-service:
    build: ./analytics-service
    container_name: analytics-service
    ports:
      - "8086:8086"
    environment:
      SPRING_PROFILES_ACTIVE: docker
    depends_on:
      - discovery-server
      - kafka
    networks:
      - smart-network

  # --------------------------------------------------------------------
  # 📋 Audit Service
  # --------------------------------------------------------------------
  audit-service:
    build: ./audit-service
    container_name: audit-service
    ports:
      - "8087:8087"
    environment:
      SPRING_PROFILES_ACTIVE: docker
    depends_on:
      - mongo
      - kafka

volumes:
  pgdata:
  mongodata:

networks:
  smart-network:
    driver: bridge
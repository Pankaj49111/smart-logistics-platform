server:
  port: 8080

spring:
  application:
    name: gateway-service
  config:
    import: optional:configserver:http://config-server:8888

  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: auth-service
          uri: http://172.21.96.1:8081
          predicates:
            - Path=/auth/**
        - id: shipment-service
          uri: lb://shipment-service
          predicates:
            - Path=/shipments/**
      default-filters:
        - AddResponseHeader=X-Gateway, SmartLogistics

eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka
    register-with-eureka: true
    fetch-registry: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
jwt:
  secret: ${JWT_SECRET:supersecurekeysmartlogistics123456789}
